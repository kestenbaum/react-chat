import React from 'react';
import MainButton from "./UI/button/MainButton";

const Login = () => {
    return (
        <div>
            <MainButton>
                Login
            </MainButton>
        </div>
    );
};

export default Login;